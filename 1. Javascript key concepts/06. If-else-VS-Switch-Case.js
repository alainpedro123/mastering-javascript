// Write a function that takes two numbers and returns the maximum of the two.

// IF-ELSE
const maxNumber = (num1, num2) =>{
	if (num1 > num2) 
		return num1 
	else
		return num2
}

// TERNORY OPERATOR
const maxNumber = (num1, num2) => {
	(num1 > num2) ? num1 : num2
}
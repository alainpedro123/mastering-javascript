// 1. HIGH-ORDER FUNCTION

let numbers = [1, 2, 3];

numbers.map(number => number * 2); // map is a High-Order Function, it takes another function as argument

setTimeout(() => console.log("Hello"), 1000); // setTimeout is also a High-Order Function